<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Half-Width Responsive Container with Overlay (Bootstrap & HTML)</title>
    <!-- Add Bootstrap CSS link -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS for responsive container and overlay -->
    <style>
        .custom-responsive-container {
            width: 50%; /* Take up half of the device's width */
            height: 450px; /* Fixed height of 450 pixels */
            max-width: 100%; /* Ensure it doesn't exceed the device width */
            position: relative; /* Required for overlay positioning */
        }

        .custom-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 50%; /* Cover half of the container */
            background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent black overlay */
            z-index: 2; /* Place the overlay above the content */
        }

        .custom-content {
            position: relative; /* Ensure content is above the overlay */
            z-index: 1; /* Place content above the overlay */
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12 p-0">
                <!-- Responsive Container -->
                <div class="custom-responsive-container bg-light">
                    <!-- Overlay for the top half -->
                    <div class="custom-overlay"></div>
                    
                    <!-- Your content goes here (box with text, YouTube video, etc.) -->
                    <!-- Example: -->
                    <div class="custom-content">
                    \iv></div></body></html>